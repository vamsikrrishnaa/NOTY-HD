# Server environment
NODE_ENV=development
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# MongoDB
MONGO_URI=mongodb://127.0.0.1:27017/notes

# 32+ chars random string
JWT_SECRET=please_change_this_to_a_long_random_secret________________________________

# Optional SMTP for sending OTP emails (fallback logs to console if omitted)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Optional Google OAuth (Web) Client ID
GOOGLE_CLIENT_ID=
